/// AppUI owns the SwiftUI view layer.  It drives disk selection through
/// DiskModel, triggers flash operations via FlashEngine, shows verification
/// progress from Verifier, and surfaces keychain status from KeychainStore.
///
/// Fill agents: implement the public API of this namespace here.
public enum AppUI {}
